<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Meet N Greet</title>
    <link rel="stylesheet" href="styles/index.css" />
    <link rel="stylesheet" href="styles/login.css" />
    <link rel="stylesheet" href="styles/cardtemplate.css" />
    <link rel="stylesheet" href="styles/input.css" />
    <script src="/socket.io/socket.io.js"></script>
  </head>
  <body>
      <div class="App">
        <header>

        </header>
        <div class="App-container">
          <div class="card-template">
            <div class="card-frame">
              <img src="imgs/logo_gray.png" class="alt-logo" alt=""/>
              <br/>
              <header class="card-title">Register</header>
              <input type="text" name="" value="">
              <button type="button" name="button" onclick="sendMessage()"></button>
              <div class="chat">
              </div>
            </div>
          </div>
        </div>
      </div>

      <script type="text/javascript">
        var socket = io.connect("/");

        socket.on("connect",function() {
          console.log('Client has connected to the server!');
        });

        socket.on("news", function (data) {
          //document.getElementById("chat").append(data);
          console.log(data);
          socket.emit("response", "this is a response");
        });

        function sendMessage() {
          socket.emit("response", "USER'S MESSAGE")
        }
      </script>
  </body>
</html>
